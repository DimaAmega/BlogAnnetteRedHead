<!DOCTYPE html>
html
  head
    meta(charset="utf-8")
    title Annette Redhead
    link(rel='stylesheet', href='/stylesheets/admin_style.css')
  body
    h1= InSistem
    .container
        form.register-form(method="post" action="/admin")
            input(type="text" name="Login" placeholder="Login")
            input(type="password" name="Password" placeholder="password")
            button(type="submit") Let me in

